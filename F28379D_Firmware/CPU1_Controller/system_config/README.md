# System configuration files 
This folder contains files required for the configuration and running of the microcontroller. 
- Memory linker file  
- Code start branch 
- Global struct definitions for bit-field programming 
- Header for peripheral memory regions  
- Clock configuration and startup C code. 